sonata_media:
    class:
        media: App\Entity\Media\Media
        gallery: App\Entity\Media\Gallery
        gallery_item: App\Entity\Media\GalleryItem
        category: App\Entity\Classification\Category
    db_driver: doctrine_orm # or doctrine_mongodb, doctrine_phpcr it is mandatory to choose one here
    default_context: default # you need to set a context
    contexts:
        default:  # the default context is mandatory
            providers:
                - sonata.media.provider.dailymotion
                - sonata.media.provider.youtube
                - sonata.media.provider.image
                - sonata.media.provider.file
                - sonata.media.provider.vimeo
            formats:
                small: { width: 100 , quality: 70}
                big: { width: 1000, height: 580, quality: 100}
                wide: {width: 1200, height: 700, quality: 100}
    cdn:
        server:
            path: /uploads/media # http://media.sonata-project.org/
    filesystem:
        local:
            directory: '%kernel.project_dir%/public/uploads/media'
            create: false